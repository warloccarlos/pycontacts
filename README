For the application to work use python3 and launch via python3 main.py
Also, make sure python3 mysql-connector is also installed. Use pip3 to install the connector.
For mysql, please make sure Authentication plugin 'caching_sha2_password' is enabled.
